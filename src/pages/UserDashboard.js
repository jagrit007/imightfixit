import React from 'react';

const UserDashboard = () => {
  return (
    <div>
      <h2>User Dashboard</h2>
      {/* Display user-specific content, orders, appointments, etc. */}
    </div>
  );
};

export default UserDashboard;
